import { config } from 'rxjs';

// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};


export const firebaseConfig = {

  apiKey: "AIzaSyDoFpDTOyqzWXpFvAkExsgNBHVEjT7noiI",
  authDomain: "cliu-2779d.firebaseapp.com",
  databaseURL: "https://cliu-2779d.firebaseio.com",
  projectId: "cliu-2779d",
  storageBucket: "cliu-2779d.appspot.com",
  messagingSenderId: "639244977879",
  appId: "1:639244977879:web:107172eefc3cbbd9"

};




/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
